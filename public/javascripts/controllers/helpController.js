angular.module('chatApp').controller('HelpController',
    ['$window', '$scope', 'socket', 'rest',
        function ($window, $scope, socket, rest) {
            var vm = this;
        }
    ]
);
